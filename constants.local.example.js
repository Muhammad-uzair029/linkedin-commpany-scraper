// Copy to `constants.local.js` — or prefer: npm run login (no cookie file needed).
// Best: copy the full Cookie header from DevTools → Network → any linkedin.com request.
export const LINKEDIN_COOKIE =
  'li_at=PASTE_HERE; JSESSIONID=PASTE_HERE; bcookie=PASTE_HERE';
